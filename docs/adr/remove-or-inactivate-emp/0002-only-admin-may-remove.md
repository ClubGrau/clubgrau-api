# Only ADMIN may Remove

Remove is irreversible anonymization of PII. MANAGER may Deactivate (`INACTIVE`) so the person stops working and can still be Reactivated; they must not Remove. Only ADMIN is Actor of Remove. The API must reject MANAGER (and EMPLOYEE) even if the client hides the button.
