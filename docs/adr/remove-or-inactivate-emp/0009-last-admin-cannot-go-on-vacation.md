# Last Admin cannot go on VACATION

Login rejects any status other than `ACTIVE`. There is no second ADMIN to restore the Last Admin, and a MANAGER cannot act on ADMINs. `VACATION` would lock the platform the same way `INACTIVE` would. Last Admin stays `ACTIVE` until another ADMIN exists. This version does not let `VACATION` users log in.
